# -*- mode: snippet; require-final-newline: nil -*-
# name: wd
# key: wd
# condition: (string= ess-dialect "R")
# --
setwd("${1:`(s-replace (getenv "HOME") "~" (if (buffer-file-name) (file-name-directory (buffer-file-name)) ""))`}")